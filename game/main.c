#include "../core/nebula.h"
#include "../core/input.h"

void game_update() {

    if (nebulaInput.left) {
        // move player left
    }

    if (nebulaInput.right) {
        // move player right
    }

    if (nebulaInput.a) {
        // pular / ação
    }

    nebula_update();
}
